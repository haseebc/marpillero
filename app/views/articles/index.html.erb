



<div class="container">
    <div class="card-standard__white text-center__simple">
    <div class="text-normal__grey--verylarge">
    <p>Latest Articles<p>
    
        <% @articles.each do |article| %>
        <div class="text-normal__greyand31px">
        <%=  article.title %>
        </div>

        <!--
        <h4 class="description-blog"> </h4>
        <%=   article.description.truncate_words(4) %>
        <h4 class="description-blog red"> </h4>
        -->
        
        <div class="button-spacing_likeblog">
            <%= link_to 'Read more.', article_path(article), class: "btn btn-xs btn-success" %>
        </div>
         
        <% end %>
    
    <% if current_user && current_user.email=="erikap02@gmail.com" %>
        <div class="newarticle-blog">
        <%= link_to 'New Article', new_article_path(@article), class: "btn btn-xs btn-primary" %>
        </div>
    <% end %>
    

    </div>

    </div>
</div>
